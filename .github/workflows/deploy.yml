name: Deploy Python App to Azure

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.10'

    - name: Create and activate virtual environment
      run: |
        python -m venv venv
        source venv/bin/activate
        pip install -r requirements.txt

    - name: Diagnosticar Secretos
      run: |
        echo "Verificando variables en la rama staging..."
        if [ -z "${{ secrets.AZURE_CLIENT_ID }}" ]; then echo "❌ AZURE_CLIENT_ID está VACÍO"; else echo "✅ AZURE_CLIENT_ID está PRESENTE"; fi
        if [ -z "${{ secrets.AZURE_TENANT_ID }}" ]; then echo "❌ AZURE_TENANT_ID está VACÍO"; else echo "✅ AZURE_TENANT_ID está PRESENTE"; fi

    - name: Log in to Azure using Client Secret
      uses: azure/login@v2
      with:
        creds: '{"clientId":"${{ secrets.AZURE_CLIENT_ID }}","clientSecret":"${{ secrets.AZURE_CLIENT_SECRET }}","subscriptionId":"${{ secrets.AZURE_SUBSCRIPTION_ID }}","tenantId":"${{ secrets.AZURE_TENANT_ID }}"}'

    - name: Deploy to Azure Web App
      uses: azure/webapps-deploy@v3
      with:
        app-name: 'cursoaz104drf'
        slot-name: 'staging'